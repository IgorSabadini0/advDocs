{
  "scripts": {
    "dev": "docker compose up -d && npx wait-on tcp:3306 && node --watch backend/src/server.js"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "express-rate-limit": "^8.5.2",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3",
    "mysql2": "^3.18.1"
  },
  "type": "module",
  "devDependencies": {
    "wait-on": "^9.0.10"
  }
}